mod cycle;
mod grammar;
mod init;
mod interpreter;
mod mapping;
mod population;
mod tree;

fn main() {

    //    evolution_cycle(rnd, 100,
    //interpret,
    //                    &get_grammar(), evaluate, crop, mutation, combine);
}
